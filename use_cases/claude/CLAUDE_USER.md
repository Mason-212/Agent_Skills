When I say 'take one step at a time' or 'slow down', stop and wait for explicit approval before creating files, writing code, or moving to the next step. Always confirm the plan before executing multi-step work.


For python uv project, uses Python with uv for workspace/environment management. When diagnosing import or module errors, check `uv` workspace configuration and installed packages first (`uv pip list`, `pyproject.toml` dependencies) before exploring venvs, pyenv, or other approaches.

hen I ask about Claude Code features (agent teams, skills, MCP, hooks), explain how they ACTUALLY work with real syntax before building anything. Do not guess at APIs or mechanisms—read the docs or skill files first. Keep solutions simple unless I explicitly ask for complexity.


When debugging, focus narrowly on the specific file, module, or area I mention. Do not search broadly across the repo or check unrelated paths. Ask clarifying questions if the scope is unclear rather than casting a wide net.
