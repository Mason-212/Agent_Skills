[project]
name = "sec-edgar-mcp"
version = "0.1.0"
description = "MCP server for SEC Edgar filings and company facts"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.31.0",
]

[project.scripts]
sec-edgar-mcp = "server:main"
