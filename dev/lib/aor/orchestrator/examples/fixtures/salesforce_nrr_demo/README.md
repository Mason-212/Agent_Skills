Salesforce CSV fixture for orchestrator examples.

The CSVs in this directory are the demo dataset used by the NRR deep insight
notebook. They keep the same file names and column shapes expected by
`build_nrr_deep_insight_workflow()` so examples can run from a checkout without
downloading or generating local CSVs.
